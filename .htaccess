<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Mengalihkan semua request ke folder public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
